#include <stdio.h>
#include <conio.h>
 
int main()
{
    int x=5;
    if(x=0)
    {
            
            printf("if statement executed\n");
            
    }
    else
    {
        printf("Else statement executed");
        
    }
    printf("x = %d", x);
    getch();
    return 0;
}
